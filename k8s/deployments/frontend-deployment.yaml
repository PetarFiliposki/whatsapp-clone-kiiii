apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  namespace: whatsapp-clone
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
      - name: frontend
        image: petar4e/whatsapp-frontend:latest  
        ports:
        - containerPort: 3001
        env:
        - name: REACT_APP_API_URL
          valueFrom:
            configMapKeyRef:
              name: app-config
              key: REACT_APP_API_URL